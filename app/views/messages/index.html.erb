<h1>タスク一覧</h1>

<ul>
  
<% @messages.each do |message| %>

    <li>
<%= link_to message.id, message %>
 : 
<%= message.content %>
</li>
  
<% end %>

</ul>

<%= link_to '新規タスクの投稿', new_message_path %>